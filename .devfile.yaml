schemaVersion: 2.2.0
metadata:
  name: devspaces-mta

components:
  - name: tools
    container:
      image: quay.io/devfile/universal-developer-image:ubi8-277c10c
      mountSources: true
      lifecycle:
        postStart:
          exec:
            command:
              - sh
              - -c
              - |
                code-server --install-extension redhat.mta-vscode-extension || true
